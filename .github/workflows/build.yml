name: build

on:
  push:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v2.3.4

      - name: prepare
        run: pwsh ./tools/prepare.ps1

      - name: build
        run: pwsh ./tools/build.ps1

      - uses: actions/upload-artifact@v2
        with:
          name: packages
          path: bin/*.nupkg

      - name: deploy
        if: ${{ github.ref_type == 'tag' }}
        run: |
          find bin -name '*.nupkg' -exec dotnet nuget push {} -s $GITHUB_SOURCE -k $GITHUB_TOKEN --skip-duplicate --force-english-output \;
          find bin -name '*.nupkg' -exec dotnet nuget push {} -k $NUGET_KEY --skip-duplicate --force-english-output \;
        shell: bash
        env:
          GITHUB_SOURCE: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
          NUGET_KEY: ${{ secrets.NUGET_API_KEY }}


